<%= form_with(model: read_cycle, local: true) do |form| %>
  <% if read_cycle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(read_cycle.errors.count, "error") %> prohibited this read_cycle from being saved:</h2>

      <ul>
        <% read_cycle.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :pagesGoal %>
    <%= form.text_field :pagesGoal %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
